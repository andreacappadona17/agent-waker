# Refuse packages published less than two days ago: compromised npm releases are
# usually caught within 24-48 hours. Enforced on every --frozen-lockfile install.
#
# minimumReleaseAgeExclude is kept empty; when blocked, pin an older version
# instead. See CONTRIBUTING.md for the security-fix exception.
minimumReleaseAge: 2880
