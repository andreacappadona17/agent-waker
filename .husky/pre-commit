# Only checks that scale with the diff, so committing stays fast.
# Repository-wide checks run on pre-push.
pnpm exec lint-staged

# After lint-staged, so it scans the bytes actually being committed.
./scripts/tool.sh gitleaks git --pre-commit --staged --redact --no-banner
